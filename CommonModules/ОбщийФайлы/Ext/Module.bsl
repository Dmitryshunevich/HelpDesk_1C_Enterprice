

Функция СформироватьИмяФайла(ИмяФайла) Экспорт
	НомерСимвола = Найти(ИмяФайла,"\");
	Если НомерСимвола<>0 Тогда
		Пока НомерСимвола<>0 Цикл
			ИмяФайла = Прав(ИмяФайла,СтрДлина(ИмяФайла)-НомерСимвола);		
			НомерСимвола = Найти(ИмяФайла,"\");
		КонецЦикла;
	КонецЕсли;
	Возврат ИмяФайла;
КонецФункции
